# Copy to .env.test and fill in. The e2e suite WIPES this account's workspace
# on every run, so point it at a throwaway login, never a real distributor.
E2E_EMAIL=you@example.com
E2E_PASSWORD=
E2E_BASE_URL=http://localhost:8080
